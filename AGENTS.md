# 项目协作规范

## 交付与版本控制

- 每次完成一个用户请求的代码或资源改动后，必须先完成与改动相称的验证。
- 验证通过后，自动创建一个内容明确的 Git 提交。
- 随后自动推送该提交到 `origin/main`；若网络或认证失败，应说明原因并在用户提供可用连接后重试。
- 不要把与当前改动无关、未被用户要求纳入版本控制的本地文件一并提交。

## 角色与建筑通行

- 建筑的入口、楼梯、露台等可走位置必须通过“建筑专属通行面”明确配置，不得仅依据贴图外观或通用地形台阶推断。
- 每个通行面要定义所在格位及高度；相邻楼梯格需形成连续的上升路径。
- 可走建筑表面需要同时解除该格的建筑碰撞，并按其高度绘制人物，避免被建筑立面或前景遮挡轮廓错误覆盖。
- 新增或调整建筑通行面后，应实际验证入口、楼梯和每层露台均可到达。
